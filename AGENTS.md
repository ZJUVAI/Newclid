# Agent Notes

- Review `CLAUDE.md` before making project changes; it contains repository-specific guidance.
- Prefer the project virtual environment for Python commands:
  - Use `.venv/bin/python` instead of system `python`.
  - Use `.venv/bin/python -m pytest` for tests.

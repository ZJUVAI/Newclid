Validated package stack for the Qwen3-1.7B PT -> SFT -> eval pipeline:

- ms-swift==3.9.3
- transformers==4.51.3
- trl==0.15.2
- peft==0.14.0
- accelerate==1.2.1
- liger-kernel==0.5.3
- tokenizers==0.21.0
- huggingface_hub==0.36.2
- modelscope==1.34.0

Runtime notes:

- Qwen3 requires transformers >= 4.51 in this setup.
- liger-kernel 0.5.3 works with transformers 4.51.3 and fixes the HybridCache import failure seen with the newer broken stack.
- flash-attn was kept as-is. Rebuild it if torch / CUDA / compiler changes.
- deepspeed custom ops may also need rebuilding if torch / CUDA changes.
- Training/eval commands in this environment should set CUDA_HOME=/usr/local/cuda-12.1.

Repository compatibility change included with this stack update:

- src/newclid/api.py now treats VLM agent imports as optional so pure-text LLM evaluation does not fail when modelscope lacks Qwen3VL symbols.
